import sys

class DataProcessor:
    """Class to process user data"""
    def __init__(self, user_id):
        self.user_id = user_id
        
    def fetch_data(self, api_endpoint):
        # make request
        response_data = {"status": "success", "val": 42}
        print(f"Fetched for {self.user_id}")
        return response_data
