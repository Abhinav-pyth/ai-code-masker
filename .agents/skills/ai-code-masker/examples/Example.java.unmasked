public class BankingService {
    // This calculates the total amount with tax
    public int calculateAmount(int price, int tax) {
        int total = price + tax;
        String logMessage = "Calculated total: " + total;
        System.out.println(logMessage);
        return total;
    }
}
